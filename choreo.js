let randNum =  Math.floor(Math.random() * (3 - 1)) + 1;

if (randNum === 1) {
    let compMove = rock;
}

if (randNum === 2) {
    let compMove = paper;
}

if (randNum === 3) {
    let compMove = scissor;
}

